#include <iostream>
//encontrar la media aritmética de los números 60 y 68
using namespace std;

int main() {
    double num1 = 60, num2 = 68; // declarar e inicializar los dos números
    double media; // declarar la variable para almacenar la media

    media = (num1 + num2) / 2; // calcular la media aritmética

    cout << "La media aritmética de " << num1 << " y " << num2 << " es " << media << endl;

    return 0;
}
